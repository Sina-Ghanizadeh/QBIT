server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback -- serves index.html for all non-file routes
    location / {
        add_header Permissions-Policy "camera=(self), microphone=(), geolocation=()" always;
        try_files $uri $uri/ /index.html;
    }

    # Proxy REST API to backend
    location /api/ {
        client_max_body_size 512k;
        proxy_pass http://backend:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy auth routes to backend
    location /auth/ {
        proxy_pass http://backend:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health endpoint not exposed to public -- silently drop with 204 No Content
    location /health {
        access_log off;
        return 204;
    }

    # Proxy Socket.io (no access_log; polling is very frequent per client)
    location /socket.io/ {
        access_log off;
        proxy_pass http://backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Proxy device WebSocket endpoint (no access_log to avoid log flood when banned devices retry)
    location /device {
        access_log off;
        proxy_pass http://backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        # Backend pings every 30s and tolerates 3 missed pongs (~90s). Allow
        # generous slack so nginx never closes an otherwise-healthy socket.
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # iOS Safari requests apple-touch-icon-precomposed.png automatically
    location = /apple-touch-icon-precomposed.png {
        access_log off;
        rewrite ^ /apple-touch-icon.png last;
    }

    # Favicon and manifest files with proper caching
    location ~ ^/(favicon\.(ico|svg)|favicon-.*\.png|apple-touch-icon\.png|site\.webmanifest)$ {
        expires 1d;
        add_header Cache-Control "public, max-age=86400";
        add_header Access-Control-Allow-Origin "*";
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
