# -----------------------------------------------------------------------------
# Main app (frontend + API)
# -----------------------------------------------------------------------------

# Google OAuth credentials (from Google Cloud Console)
GOOGLE_CLIENT_ID={YOUR_GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET={YOUR_GOOGLE_CLIENT_SECRET}
GOOGLE_CALLBACK_URL=https://{YOUR_API_DOMAIN}/auth/google/callback

# Cookie domain (parent domain shared by frontend and API)
COOKIE_DOMAIN=.{YOUR_DOMAIN}

# Frontend URL (for CORS and OAuth redirect)
FRONTEND_URL=https://{YOUR_FRONTEND_DOMAIN}

# -----------------------------------------------------------------------------
# Device WebSocket (QBIT hardware)
# -----------------------------------------------------------------------------

# Device API key -- ESP32 devices must send this key to connect.
# Generate a random string (e.g. openssl rand -hex 32).
# This SAME value must be set in firmware/src/main.cpp WS_API_KEY.
DEVICE_API_KEY={YOUR_DEVICE_API_KEY}

# Max simultaneous device WebSocket connections (default: 100)
MAX_DEVICE_CONNECTIONS=100

# -----------------------------------------------------------------------------
# Admin UI (port 3002; session-based, httpOnly cookie, 24h expiry)
# -----------------------------------------------------------------------------

# Admin login. If both set, admin page shows login modal; session stored server-side.
# Username: 1-64 characters. Password: 8-128 characters.
# Leave empty to disable admin auth (not recommended if 3002 is reachable).
ADMIN_USERNAME={YOUR_ADMIN_USERNAME}
ADMIN_PASSWORD={YOUR_ADMIN_PASSWORD}

# -----------------------------------------------------------------------------
# Optional local user login (self-host without Google OAuth)
# -----------------------------------------------------------------------------
# When both are set, the web UI shows a username/password login.
# Password must be at least 8 characters.
# LOCAL_AUTH_USERNAME=qbit
# LOCAL_AUTH_PASSWORD=change-me-please

# Allow self-registration of local accounts (default: true). Set false to disable Sign up.
# LOCAL_REGISTRATION_ENABLED=true

# -----------------------------------------------------------------------------
# Telegram bot (optional — phase 1 messenger integration)
# -----------------------------------------------------------------------------
# Create a bot via @BotFather, then set the token. Username is for help text.
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_BOT_USERNAME=YourQbitBot
